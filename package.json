{
  "name": "redux-detector",
  "version": "0.5.0",
  "description": "Redux extension that allows to dispatch next actions after detected state change.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf dist && rimraf lib",
    "compile": "rimraf dist && tsc --pretty",
    "build:umd": "npm run compile && cross-env NODE_ENV=development webpack lib/index.js",
    "build:umd:min": "npm run compile && cross-env NODE_ENV=production webpack lib/index.min.js",
    "build": "npm run build:umd && npm run build:umd:min",
    "prepublish": "npm run clean && npm run build"
  },
  "repository": {
    "url": "https://github.com/piotr-oles/redux-detector.git",
    "type": "git"
  },
  "keywords": [
    "redux",
    "reducer",
    "state",
    "predictable",
    "functional",
    "immutable",
    "detector",
    "change",
    "flux",
    "elm"
  ],
  "author": "Piotr Oleś <piotrek.oles@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/piotr-oles/redux-detector/issues"
  },
  "dependencies": {
    "redux": "^3.6.0"
  },
  "devDependencies": {
    "cross-env": "^3.1.4",
    "rimraf": "^2.5.4",
    "source-map-loader": "^0.1.5",
    "typescript": "^2.1.4",
    "webpack": "^1.14.0"
  },
  "npmName": "redux-detector",
  "npmFileMap": [
    {
      "basePath": "/lib/",
      "files": [
        "*.js"
      ]
    }
  ]
}
